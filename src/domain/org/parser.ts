import type { OrgNode, OrgFile } from './ast';

export class OrgParser {
    parse(text: string): OrgFile {
        // TODO: Implement parser
        return {
            nodes: [],
            metadata: {},
        };
    }
}
